config:
  target: "http://localhost:5000"
  phases:
    -
      duration: 60
      arrivalRate: 20
scenarios:
  -
    name: "Create and Get"
    flow:
      -
        post:
          url: "/api/entities/create"
          capture:
            json: "$.guid"
            as: "guid"
      -
        get:
          url: "/api/entities/{{ guid }}"